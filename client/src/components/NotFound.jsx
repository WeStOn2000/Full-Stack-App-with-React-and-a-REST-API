const NotFound = () => {
    return (
        <main>
            <div className="wrap">
                <h2>Not Found</h2>
                <p>Sorry! We can not find the page you are looking for.</p>
            </div>
        </main>
    );
};

export default NotFound;